# Planning docs and internal files (not for public serving)
HANDOFF.md
SESSION-LOG.md
DEVLOG.md
_docs/
_work/
# Claude/Cursor local settings — never in the public tree.
# tools/hooks/session-start.sh still lives in-repo; point a local settings.json at it.
.claude/

# Funnel Worker — local secrets + dependencies (never serve from Pages)
worker/.dev.vars
worker/.dev.vars.*
worker/node_modules/
worker/dist/

# AI Voice/Conversation Widget + CRM project: tracked on feature branch so
# files sync back, but never commit secrets or build artifacts.
AI Voice and Conversation Widget And CRM Project/**/.dev.vars
AI Voice and Conversation Widget And CRM Project/**/.env
AI Voice and Conversation Widget And CRM Project/**/.env.*
AI Voice and Conversation Widget And CRM Project/**/node_modules/
AI Voice and Conversation Widget And CRM Project/**/.wrangler/

# OS artifacts
.DS_Store
Thumbs.db
desktop.ini
*.~lock.*

# Python tooling bytecode
__pycache__/
*.py[cod]

# FUSE mount artifacts (leaked from Cowork sandbox)
.fuse_hidden*

# Original Landfall PNGs (converted to WebP, kept locally only)
images/LandFall*.png
images/IM LandFall*.png

# Lamprey binary installers (too large for git - host elsewhere)
lamprey/downloads/

# Cloudflare Wrangler local dev cache
.wrangler/

# Local credentials for the voice-scheduling work — NEVER commit
AI Voice and Conversation Widget And CRM Project/Vapi and cal keys.md
*Vapi and cal keys*.md

# Local scratch / source artifacts — kept locally, never serve from Pages
# Raw icon art (multi-MB PNGs) lives here; images/ carries only the web-ready
# webp derived from it via the style guide's How-To A1.
_icon-sources/
Lamprey Crossover Icon Assets/
images/*.bak
ChatGPT Image*.png
IM Logo*.png
IM-logo6-cut.png
images/IM-logo6-cut.png
Island Mountain 20-Sided.png
IslandMountain_D20.*
images/single-user.png
skull-crossbones.png
# Source master art — the title-cased originals behind the served webps. Kept in
# images/ as the working masters, never committed (the web-ready webp ships instead).
images/* *.png
images/IM-Deploys-.png
# Icon pipeline scratch — PNG source art and webp outputs staged in icons/. Nothing
# committed references icons/ image assets (the site serves icons from images/ and the
# tracked remixicon font), so this whole staging area stays local, never committed.
icons/*.png
icons/*.webp
icons/*.jpg
icons/masters/

# Credential/keys notes (NEVER commit)
AI Voice and Conversation Widget And CRM Project/Twilio Keys.md
AI Voice and Conversation Widget And CRM Project/Vapi and cal keys.md
**/*Keys*.md
**/*keys*.md
.twilio-*.json

# Local Codex/image-generation scratch and voice API inspection dumps
.codex/
.tmp/
worker/.vapi-*.json

# Raw demo export exceeds GitHub's 100 MB object limit; publish through R2/CDN instead
Lamprey Demo 7.13.26.mp4

# Planning docs (PSPRs) — private, never published to the public repo or Pages
PLANNING/

# LinkedIn six-week campaign — internal working files, plans, and icon cards; never in the public repo
/linkedin-six-week-authority-campaign-2026-08-10/
/cards/

# Workshop dumps and agent/session docs — keep out of the public GitHub tree.
# Scoped to repo root (leading /) so images/, video/, worker/, and tools/ stay commitable.
*.bak
/IMG_*.jpeg
/IMG_*.jpg
/IMG_*.JPG
/IMG_*.GIF
/IMG_*.gif
/Screenshot *
/Generated image *
gemini_generated_*
* - Copy.*
/*.docx
/CoWork *
/AI Voice and Conversation Widget And CRM Project/
/Agentic Orchestration and Woven Security Fabric/
/Screenshots/
/_pml-updates/
/assets-src/
/AGENTS.md
/CLAUDE.md
/INDEX.md
/SESSION-RULES.md
/CANON.md
/BRAND.md
/CONVENTIONS.md
/ARCHITECTURE.md
/DEPLOY.md
/PRODUCTS.md
/PROJECT.md
/VERTICALS.md
/SEO-STATE.md
/AURORA-PRESENTATION-RESEARCH.md
/ICON-BACKLOG.md
/ICON-MAPPING.md
/MOBILE-CONTENDER-DEVLOG.md
/STYLE-CONTINUITY-AUDIT.md
/TRAFFIC-AND-HOOK-AUDIT.md
/VIDEO_BRANDING_WORKFLOW.md
/ISLAND-MOUNTAIN-VISION-MISSION.md
/THE-ISLAND-MOUNTAIN-DOCTRINE.md
/SOVEREIGNTY-HANDOFF.md
/SOVEREIGNTY-DOCKER-GATE-PROMPT.md
/Long-running Agents.pdf
/MEMRI Promo *

# Cold outreach templates — internal sales assets, never in the public repo (this repo is PUBLIC)
/Cold Outreach Templates/

# ─────────────────────────────────────────────────────────────────────────
# Financial artifacts — LOCAL ONLY, NEVER on GitHub (this repo is PUBLIC).
# Pro forma, COGS, BOM, financial models + their decks/intakes/reviews/locks.
# This repo is a website; no spreadsheet is ever meant to be served or tracked.
*.xlsx
*.xlsm
*.xlsb
*PRO-FORMA*
*Pro-Forma*
*pro-forma*
*proforma*
*PROFORMA*
*COGS*
*cogs*
*-BOM.*
*BOM*.xlsx
*Financial-Model*
*financial-model*
IM-PRO-FORMA*
# Business plan now carries the raise + financial tables — treat as a financial artifact.
*BUSINESS-PLAN*
*Business-Plan*
*business-plan*
bom_preview.html
# Office/temp lock files (~$… shadow files)
~$*

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# ─────────────────────────────────────────────────────────────────────────
# Private local materials — this repo is PUBLIC (GitHub Pages from main).
# Entity filings, tax letters, TTX/SitMan packages, and named private papers.
/Formation Documents/
/Emergency Management White Paper and TTX SitMan/
*Cascadian*Shadow*
*Cascadian_Shadow*

# Loose private papers that keep landing in the site root
/30 Content Ideas for Basho Parks.docx
/Abstract_Moving_Ceiling.docx
/Author_Bio_Moving_Ceiling.docx
/LINKEDIN-CAMPAIGN-AUTOPSY-*.md
/NVIDIA-PITCH-DECK-CLAUDE-DESIGN-PROMPT.md
/Local_AI_for_a_bad_Tuesday.m4a
/blog-draft-*.md

# Live-worker inspection dumps (assistant IDs, webhook secrets, full prompts)
worker/.audit-*.json
